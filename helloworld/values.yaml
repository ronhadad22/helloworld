kubernetes:
  host: https://kubernetes.default

helloworld:
  namespace: default
  image:
    name: hello-world
    tag: latest
    pullPolicy: IfNotPresent
  service:
    ingressClass: internal-ingress
